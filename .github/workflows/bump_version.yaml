name: Call a reusable workflow

on:
   schedule:
    # The "*" (#42, asterisk) character has special semantics in YAML, so this
    # string has to be quoted.
    - cron: '0 0 */2 * *'
   workflow_dispatch:

jobs:
  call-workflow-passing-data:
    permissions:
      contents: write
      pull-requests: write

    uses: GlueOps/github-workflows/.github/workflows/bump-version-cut-release.yaml@7b077a4cf0bfbd34eaf0a5f54e4eccedade51606 # main
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
